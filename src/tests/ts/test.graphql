query User {
  user(id: "1") {
    id
    name
  }
}
mutation UpdateUser {
  userUpdate(id: "2") {
    id
    name
  }
}
query User {
  user(id: "3") {
    id
  }
}
query User {
  user(id: "4") {
    id
  }
}
