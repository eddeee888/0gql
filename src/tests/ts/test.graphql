query User {
  user(id: "420") {
    id
    name
  }
}
mutation UpdateUser {
  userUpdate(id: "420") {
    id
    name
  }
}
query User {
  user(id: "500") {
    id
  }
}
query User {
  user(id: "501") {
    id
  }
}
